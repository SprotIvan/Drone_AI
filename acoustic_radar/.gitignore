# Аудіо та датасети (важкі, генеруються локально)
raw_audio/
dataset/
*.zip
*.zip.part

# Моделі та калібрування конкретної установки
best_model.pth
acoustic_radar.onnx
acoustic_radar.onnx.data
model_config.json
radar_calibration.json

# Unified station: runtime output
logs/                  # rotating station.log
detections/            # WAV clips saved around each alarm
_hud_preview/          # regenerated by `python hud.py`
_radar_preview/        # regenerated by `python radar_overlay.py`
fusion_config.json     # per-installation overrides (like radar_calibration.json)

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
